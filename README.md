Sistema de votaciones (test)

Sistema para votar por diferentes productos (test).

Instalación

Instalamos nuestras dependencias:

```bash
npm install
```

Ejecutamos nuestro proyecto con vue 3:

```bash
npm run dev
```

